inferenceExtension:
  replicas: 1
  flags:
    # in vLLM 10.0+, the metric is renamed while upstream GAIE is still using the old name as default.
    # See https://github.com/kubernetes-sigs/gateway-api-inference-extension/pull/1905.
    kv-cache-usage-percentage-metric: "vllm:kv_cache_usage_perc"
    v: 4 # log verbosity
  image:
    # both downstream infernece-scheduler and upstream epp images can support precise KV Cache awareness based on the configurations here
    ###################
    name: llm-d-inference-scheduler
    #hub: ghcr.io/llm-d
    #tag: v0.4.0
    hub: quay.io/sallyom
    tag: tracing-0.5
    ###################
    # name: epp
    # hub: registry.k8s.io/gateway-api-inference-extension
    # tag: v1.2.0
    ###################
    pullPolicy: Always
  extProcPort: 9002
  # ZMQ port for `kvevents.Pool` (KVEvents subscriber)
  extraContainerPorts:
    - name: zmq
      containerPort: 5557
      protocol: TCP
  extraServicePorts:
    - name: zmq
      port: 5557
      targetPort: 5557
      protocol: TCP
  # huggingface token for tokenizer
  env:
    - name: HF_TOKEN
      valueFrom:
        secretKeyRef:
          name: llm-d-hf-token
          key: HF_TOKEN

  pluginsConfigFile: "precise-prefix-cache-config.yaml"
  pluginsCustomConfig:
    precise-prefix-cache-config.yaml: |
      apiVersion: inference.networking.x-k8s.io/v1alpha1
      kind: EndpointPickerConfig
      plugins:
        - type: single-profile-handler
        - type: precise-prefix-cache-scorer
          parameters:
            indexerConfig:
              tokenProcessorConfig:
                blockSize: 64
                hashSeed: "42"
              tokenizersPoolConfig:
                modelName: "meta-llama/Llama-3.1-8B-Instruct"
                hf:
                  tokenizersCacheDir: "/tmp/tokenizers"
        - type: kv-cache-utilization-scorer
        - type: queue-scorer
        - type: max-score-picker
      schedulingProfiles:
        - name: default
          plugins:
            - pluginRef: precise-prefix-cache-scorer
              weight: 3.0
            - pluginRef: kv-cache-utilization-scorer
              weight: 2.0
            - pluginRef: queue-scorer
              weight: 2.0
            - pluginRef: max-score-picker

  # Monitoring configuration for EPP
  monitoring:
    secret:
      name: kv-events-gateway-sa-metrics-reader-secret
    interval: "10s"
    # Prometheus ServiceMonitor will be created when enabled for EPP metrics collection
    prometheus:
      enabled: true
      auth:
        # To allow unauthenticated /metrics access (e.g., for debugging with curl), set to false
        enabled: true

  # OpenTelemetry distributed tracing configuration
  # Set enabled: true to enable distributed tracing for EPP/GAIE
  # Override collector endpoint via helmfile set or by editing this value
  tracing:
    enabled: true  # Set to true to enable distributed tracing
    # OTLP endpoint for trace export
    otelExporterEndpoint: "http://llm-d-collector-collector.observability-hub.svc.cluster.local:4317"
    #otelExporterEndpoint: "http://opentelemetry-collector.monitoring.svc.cluster.local:4317"
    sampling:
      # Parent-based sampling with 100% trace ratio
      sampler: "parentbased_traceidratio"
      samplerArg: "1.0"

inferencePool:
  targetPorts:
    - number: 8000
  modelServerType: vllm
  modelServers:
    matchLabels:
      llm-d.ai/inferenceServing: "true"
